_schema-version: 3.3.0
ID: CAPLLM
description: A simple CAP project.
version: 1.0.0
modules:
- name: CAPLLM-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: CAPLLM-auth
  - name: CAPLLM-db
  - name: CAPLLM-connectivity
  - name: CAPLLM-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    readiness-health-check-http-endpoint: /health
    readiness-health-check-type: http
  build-parameters:
    builder: npm
- name: CAPLLM-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: CAPLLM-db
  parameters:
    buildpack: nodejs_buildpack
- name: CAPLLM-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: CAPLLM-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - capllmui.zip
      name: capllmui
      target-path: resources/
- name: capllmui
  type: html5
  path: capllm-ui/capllm-ui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: CAPLLM-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: CAPLLM-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: CAPLLM-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: CAPLLM-connectivity
  type: org.cloudfoundry.managed-service
  parameters:
    service: connectivity
    service-plan: lite
- name: CAPLLM-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: CAPLLM-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: CAPLLM-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
